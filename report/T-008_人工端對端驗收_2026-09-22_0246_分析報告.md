---
type: task-report
id: T-008
name: 人工端對端驗收
status: ⏸️ 暫停
retry_count: 0
pause_reason: 等待人工決策
started_at: 2026-09-22_0245
generated_at: 2026-09-22_0246
---

# T-008 人工端對端驗收

## 基本資訊

| 項目 | 內容 |
|------|------|
| 任務 ID | T-008 |
| 分支 | `feat/多席位分頁綁定` |
| 任務類型 | 實作（驗收） |
| 狀態 | ⏸️ 暫停 |
| 暫停原因 | 等待人工決策（需真人操作兩個編輯器視窗與瀏覽器，AI 無法代跑） |
| 重試次數 | 0 |

## 為什麼暫停

T-008 的 9 項驗收裡，有 6 項本質上需要真人：要裝擴充、開兩個編輯器視窗、看瀏覽器分頁的
實際行為、確認 Chrome profile 的登入狀態還在。這不是資訊不足，也不是原地打轉，是任務
性質使然。

**但不該就這樣丟回去。** 暫停前我先把「機制層可自動驗證的部分」真的跑起來了，把需要
人工的範圍縮到最小。

## 已自動驗證的部分（新增，非原派工單內容）

寫了 [test/seat-check.js](../test/seat-check.js)（`test/` 不進版控，留在工作樹），用真的
`openBridge()` 把多個席位的橋同時開起來，走臨時鎖目錄不碰 `~/.chatgpt-bridge/ports/`。
因為 8765 當下被執行中的 hadome 佔著，改用 8766 / 8770 / 8771 三個席位。

重跑方式：

```bash
node test/seat-check.js
```

實測輸出（2026-09-22 02:45，exit 0）：

```
✅ 兩個席位的主橋同時 listen — A=8766 B=8770
✅ 兩份鎖檔都寫出來了 — 鎖=8766,8770
✅ 鎖檔記錄了各自的 workspace — /專案/A / /專案/B
✅ 子代理埠區塊不重疊 — A=8814,8815 B=8818,8819
✅ 起始沒有 claim.json
✅ 配對模式等了 7 秒仍不寫 claim.json（不會去搶別人的分頁）
✅ 反面對照：非配對模式確實會寫 claim.json — {"port":8771,...,"workspace":"/專案/C",...}
✅ 關閉後鎖檔清乾淨 — 剩 0 份

全部通過
```

### 這組測試為什麼可信

第 7 項是**反面對照**：如果把 `paired` 關掉，同樣的等待流程確實會寫出 `claim.json`。
沒有這一條，第 6 項的「沒寫 claim」可能只是因為程式根本沒跑到那段——反面對照證明
claim 機制是活的，而配對模式確實把它關掉了。

等待時間 7 秒是刻意選的：`CLAIM_AFTER_MS` 是 5000，7 秒確定越過了寫 claim 的時間點。

## 驗收項現況

| # | 驗收項 | 狀態 | 依據 |
|---|--------|------|------|
| 1 | slot 0 回歸（普通分頁連 8765） | ⏸️ 待人工 | 需真實分頁 |
| 2 | slot 0 子代理開在 8810/8811 | ⏸️ 待人工 | 需真實分頁；埠位計算已由 seat-check 證實 |
| 3 | slot 0 瀏覽器仍用 9444 與原 profile（登入還在） | ⏸️ 待人工 | 需真的起瀏覽器 |
| 4 | 多開：第二視窗換席、`pairTab` 開出分頁 | 🟡 部分 | 「兩席同時 listen + 鎖檔正確」已自動驗證；`pairTab` 開瀏覽器與面板訊息待人工 |
| 5 | **不互搶（claim 不出現）** | ✅ **已自動驗證** | seat-check 第 6 項 + 反面對照 |
| 6 | 不漂移（孤兒分頁不跑去接 8765） | ⏸️ 待人工 | 需真實瀏覽器分頁跑 content.js |
| 7 | 子代理不撞 | 🟡 部分 | 埠區塊不重疊已自動驗證；四份結果都回得來待人工 |
| 8 | 瀏覽器隔離（關一邊不殺另一邊） | ⏸️ 待人工 | 需起兩顆瀏覽器 |
| 9 | 滿席提示 | ⏸️ 待人工 | 需第 5 個視窗，且要看面板訊息 |
| — | T-006 的提示時機（8 秒是否合適） | ⏸️ 待人工 | 需真實面板 |

**已自動驗證 1 項、部分驗證 2 項、待人工 7 項。**

## 人工驗收怎麼做

先建置與安裝（**兩邊都要重載，protocol 已升到 61**）：

```bash
npm run build && npm run package:chrome
# chrome://extensions 重新載入擴充 → chatgpt.com 分頁也要重載
```

然後依派工單 [todo/todo.md](../todo/todo.md) 的 T-008 表格逐項做。最該優先確認的是
**第 1 項（slot 0 回歸）**——那是「沒弄壞現有單視窗使用者」的唯一保證，其餘都是新功能。

## 建議方向

1. **先做第 1 項**。若 slot 0 回歸有問題，後面全部不用看，直接回頭修。
2. 第 6 項（不漂移）要看瀏覽器 console，關鍵字是「このタブは枠 8766 と組んでいます」。
3. 第 8 項若發現 profile 尾碼導致要重新登入，那是預期行為——席 1 以後用的是獨立 profile，
   本來就要各自登入一次。這點 README 沒寫，實測後若覺得會困擾，值得補一句。
4. T-006 的 8 秒若太早（釘住的分頁因退避常要更久才接上），改 `PAIR_HINT_MS` 即可。

## 交接備註

- 本工單暫停不影響已完成的 T-001~T-007，程式碼已 commit 且可建置可打包。
- T-009（PR）原本就排在 T-008 之後，使用者也明確說過「先不做 pr」，一併暫停。
- 恢復方式：人工跑完驗收後，把派工單 T-008 狀態改回 `待執行` 或直接標 `✅ 完成`。
